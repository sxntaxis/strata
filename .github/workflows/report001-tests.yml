name: report001-tests

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize

permissions:
  contents: write

jobs:
  certify:
    if: github.event.pull_request.head.ref == 'report/001-ranges-active-ics-ordering'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout implementation branch
        uses: actions/checkout@v4
        with:
          ref: report/001-ranges-active-ics-ordering
          fetch-depth: 0

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Apply focused tests
        run: |
          base64 --decode tools/report001-tests.patch.b64 | gzip --decompress > /tmp/report001-tests.patch
          git apply --check /tmp/report001-tests.patch
          git apply /tmp/report001-tests.patch

      - name: Format
        run: cargo fmt --all

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run strict Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Run tests
        run: cargo test --all-features

      - name: Commit focused tests
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/cli.rs src/domain.rs tests/cli_lifecycle.rs
          git commit -m 'REPORT-001: certify report and export contracts'
          git push origin HEAD:report/001-ranges-active-ics-ordering
