name: reconciliation001b2b-archived-recovery

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: write

jobs:
  custody:
    if: github.head_ref == 'reconciliation/001b2b-legacy-finish-receipts'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: reconciliation/001b2b-legacy-finish-receipts
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Apply archived recovery custody fix
        run: python tools/reconciliation001b2b-archived-recovery.py
      - name: Format
        run: cargo fmt --all
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Strict Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: Test
        run: cargo test --all-targets --all-features
      - name: Commit clean custody fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(reconciliation-001b2b): preserve archived recovery custody"
          git push origin HEAD:reconciliation/001b2b-legacy-finish-receipts
