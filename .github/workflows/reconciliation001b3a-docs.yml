name: reconciliation001b3a-docs

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: reconciliation/001b3a-atomic-initial-active
          fetch-depth: 0

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Promote B3A authority
        run: python tools/reconciliation001b3a-docs.py

      - name: Remove temporary documentation machinery
        run: |
          rm -f tools/reconciliation001b3a-docs.py
          rm -f .github/workflows/reconciliation001b3a-docs.yml

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Strict Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Test
        run: cargo test --all-targets --all-features

      - name: Commit accepted authority
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs(reconciliation-001b3a): accept atomic initial generation"
          git push origin HEAD:reconciliation/001b3a-atomic-initial-active
