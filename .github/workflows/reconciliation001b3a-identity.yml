name: reconciliation001b3a-identity

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  identity:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: reconciliation/001b3a-atomic-initial-active
          fetch-depth: 0

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Apply bootstrap identity correction
        run: python tools/reconciliation001b3a-identity.py

      - name: Remove temporary machinery
        run: |
          rm -f tools/reconciliation001b3a-identity.py
          rm -f .github/workflows/reconciliation001b3a-identity.yml

      - name: Format
        run: cargo fmt --all

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Strict Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Test
        run: cargo test --all-targets --all-features

      - name: Commit clean identity correction
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(reconciliation-001b3a): bind checkpoint payload identity"
          git push origin HEAD:reconciliation/001b3a-atomic-initial-active
