name: temporal002-docs

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize

permissions:
  contents: write

jobs:
  document:
    if: github.event.pull_request.head.ref == 'temporal/002-interval-boundaries'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout implementation branch
        uses: actions/checkout@v4
        with:
          ref: temporal/002-interval-boundaries
          fetch-depth: 0

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Apply durable documentation
        run: python tools/temporal002_docs.py

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run strict Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Run tests
        run: cargo test --all-features

      - name: Commit certified documentation
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add README.md docs/ARCHITECTURE.md docs/DECISIONS.md docs/TEMPORAL_AUTHORITY.md notebook/NOW.md notebook/work/ISSUE-RECONCILIATION-001.md notebook/work/RELIABILITY-001-persistence-and-audit-remediation.md
          git commit -m 'TEMPORAL-002: record interval authority'
          git push origin HEAD:temporal/002-interval-boundaries
