name: reconciliation001b2b-promote

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: write

jobs:
  promote:
    if: github.head_ref == 'reconciliation/001b2b-legacy-finish-receipts'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: reconciliation/001b2b-legacy-finish-receipts
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Promote B2B authority
        run: python tools/reconciliation001b2b-promote.py
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Strict Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: Test
        run: cargo test --all-targets --all-features
      - name: Commit clean promotion
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs(reconciliation-001b2b): promote legacy finish authority"
          git push origin HEAD:reconciliation/001b2b-legacy-finish-receipts
