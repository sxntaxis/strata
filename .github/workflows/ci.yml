name: ci

on:
  pull_request:

permissions:
  contents: write

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: reconciliation/002-close-all-issues
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Promote accepted authority
        run: python3 .github/scripts/promote_authority_002.py

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Strict Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Full suite
        run: cargo test --all-features

      - name: Publish clean authority commit
        run: |
          rm .github/authority002-trigger.txt .github/scripts/promote_authority_002.py
          cat > .github/workflows/ci.yml <<'YAML'
          name: ci

          on:
            push:
              branches: [main]
            pull_request:

          jobs:
            checks:
              runs-on: ubuntu-latest
              steps:
                - name: Checkout
                  uses: actions/checkout@v4

                - name: Set up Rust
                  uses: dtolnay/rust-toolchain@stable
                  with:
                    components: rustfmt, clippy

                - name: Cache cargo
                  uses: Swatinem/rust-cache@v2

                - name: Check formatting
                  run: cargo fmt --all -- --check

                - name: Run clippy
                  run: cargo clippy --all-targets --all-features -- -D warnings

                - name: Run tests
                  run: cargo test --all-features
          YAML
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "docs(authority): close post-SQLite issue reconciliation"
          git push origin HEAD:reconciliation/002-close-all-issues
